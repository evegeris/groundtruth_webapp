'use strict';

// Register `polygonDraw` component, along with its associated controller and template
angular.
  module('polygonDraw').
  component('polygonDraw', {
    templateUrl: 'polygon-draw/polygon-draw.template.html',
      controller: function PolygonDrawController() {
      this.examples = [
        {
          name: 'Testing Name'
        }, {
          name: 'Motorola XOOM™ with Wi-Fi'
        }, {
          name: 'MOTOROLA XOOM™'
        }
      ];
    }
  });
